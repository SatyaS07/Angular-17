Git is an application which needs to be download and install, which is used to track version.. it is a version control system
https://git-scm.com/download
windows 64 bit
after installing check git -v
"https://github.com/SatyaS07/Angular-17"
Github is just a online code storage like gdrive. You can also use bitbucket, gitlab, aws

Step1: download the git from https://git-scm.com/download and install it and check the version by "git -v" in cmd
Step2: create github account
Step3: go to vs code studio and login into github
step4: setup config in vscode
git config --global user.email "youremail"
git config --global user.name "username"

steps:
1. git init 
2. git add .
3. git commit -m "update"
4. git remote add origin "link"
5. git push -u origin main